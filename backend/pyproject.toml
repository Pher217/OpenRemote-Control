[project]
name = "agent-command-center-backend"
version = "0.0.1"
description = "Backend for the Agent Command Center"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling>=1.20"]
build-backend = "hatchling.build"
